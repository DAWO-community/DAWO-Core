# Profiles
Profile management and user-specific environment overrides.

## Key Files
- `profiles/` contains `.nix` files for specific environments or roles.

## Responsibilities
- Dynamic configuration based on machine discovery (LLVM, GPUs).
- User-level profile inheritance.
- Environment persistence across sessions.

# Networking
The core network stack configuration for the entire infrastructure.

## Key Files
- `networking/` contains VPN, WiFi, Routing (BGP/OSPF), and Bridge configurations.

## Responsibilities
- IP address management (IPAM).
- DNS over TLS / local DNS forwarders.
- SSH keys for automated deployment.
- Firewall rules (NFTables/Iptables).

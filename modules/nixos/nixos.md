# NixOS Base
Low-level system modifications and global NixOS definitions.

## Key Files
- `nixos/` contains core options that affect every machine in the flake.

## Responsibilities
- Rebuild triggers.
- System-wide package lists (global).
- KVM / Docker runtime setups used across various hosts.
